# Test Data

This directory contains minimal test files for integration testing.

## Structure

- `text/` - Text files (`.txt`, `.md`, `.go`)
- `binary/` - Binary files (`.png`, etc.)
- `symlinks/` - Symlink test cases
- `special/` - Files with special characters
- `permissions/` - Files with different permissions
